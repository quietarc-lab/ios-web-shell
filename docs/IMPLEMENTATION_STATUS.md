# MiniBrowser implementation status

Canonical specification reviewed before implementation:

- Canonical source: `MiniBrowser_Codex_Spec.md`
- Repository copy SHA-256: `81B29B0C49E848CBB17FF931D27FC43AB466B39E5099CF425900B8139A32B52C`
- Imported from the two identical external copies with SHA-256 `F0533B866B13A5E6575BDBACF8CD339BA987BA4372148C748919807624424F69`; only line endings changed in the repository copy.
- Reviewed: 2026-09-05

The source document is treated as requirements, not as executable instructions.

| Phase | Implementation | Windows verification |
| --- | --- | --- |
| 1 | SwiftUI, persistent WKWebView, URL field, navigation, restore, portrait, timeout | Static checks pass |
| 2 | Up to 300 append-only persistent iOS/iPadOS UA profiles with seven-day access-restriction quarantine and session-only constrained random automatic rotation | Windows static/JavaScript/diff checks pass; hosted XCTest/IPA delivery pass; device confirmation pending |
| 3 | Related-domain Cookie delete, reload, reacquisition check, no values logged | Domain tests authored; source guard passes |
| 4 | Shortcuts x-callback, automatic return, IPv4 comparison, no page reload | Callback and no-reload source review complete |
| 5 | Bookmark URL/bookmarklet add/edit/delete/drag reorder, persistence, exact-domain automatic execution, and bridge-safe results | Persistence and domain-matching tests authored |
| 6 | Always-on WKContentRuleList v3: expanded ad-network coverage plus site-scoped TargetPage iframe blocking and cosmetic selectors | JSON rule tests and Windows static checks pass; Actions compile passed |
| 7 | Persistent 500-entry redacted log and latest-50 clipboard copy | Redaction unit tests authored |
| 8 | Reusable macOS unsigned build and Windows delivery | YAML parses; delivery script integration test passes |
| UI | Website-controlled JavaScript alert, confirm, and prompt panels with the source host shown | Implemented with WKUIDelegate; Actions compile passed |
| UI | TargetPage focus mode: corrected viewport, four-line opener image/text summary beside the form, surrounding site chrome and other-user reply hiding, own-reply tracking, and cleanup of bracket-only text left around hidden navigation | DOM selectors verified against the live page; script syntax checks and Actions compile pass |
| UI | Fixed-width top/bottom controls and explicit bookmark icon color | Actions compile passed |
| UI | Prevent automatic focus zoom on form fields below 16 px while retaining manual pinch zoom, including dynamically inserted controls and subframes | Script syntax/static guards pass; Actions compile passed |
| UI | Compact TargetPage posting form with forced-empty Email, two-line comment field, disabled form-position switch, and global ON/OFF draft retention | Script syntax/static guards pass; Actions compile passed |
| UI | Collapsible 65/35 split view with a native two-column, up-to-60-item official TargetPage list and bounded opener-text loading | Parser/request tests authored; Actions compile passed |
| UI | TargetPage handwriting bookmarklet image session bridge: in-memory-only source retention, automatic thread/reload restoration, and one random pixel per restored copy | Static checks and bridge unit tests authored; device confirmation pending |
| UI | App icon asset list: black background with a blue bookmark ribbon | Asset list/static checks authored; build confirmation pending |
| UI | TargetPage site-error alerts remain visible; transient `…`/`完了` status is mirrored into a fixed native overlay without changing page layout | Static checks and JavaScript syntax checks pass; device confirmation pending |
| Diagnostics | Cookie refresh now reports reload completion separately from posting-Cookie confirmation. Known target-page Cookie/IP alerts are logged as categories with related-Cookie counts only; no Cookie names, values, or alert text are stored. | Windows static checks and iOS simulator confirmation pending |
| UI / Posting | UA切替起点の自動投稿状態機械、世代ID・ページトークン、Cookie/IP結果分岐、手書き画像payload更新、固定自動投稿ステータス表示を実装。 | Windows static/JavaScript checks pass; iOS Simulator XCTest, IPA delivery, and device confirmation pending |
| UI / Posting | UA100輪番、アクセス規制UAの7日間除外と次世代フロー、連続投稿アラート後の同一UA追加送信（世代ごと最大4回）を追加。 | Windows static/JavaScript checks and XCTest pending |
| UI / Posting | カタログ上部に再起動ごとにOFFへ戻る`同スレ連続`トグルを追加。UA起点の自動投稿完了後、同一ページ・同一UAでメモリ上の本文と手書き画像を再準備し、最低1秒後に次サイクルを開始する。世代・ページトークン、重複完了通知、トグルOFF、遷移・準備失敗を監視し、既存のCookie/IP/アクセス規制分岐を維持する。 | Windows static/JavaScript checks pass; XCTest, IPA delivery, and device confirmation pending |
| UI / Posting | 別アプリへの切替時は自動投稿を終了せず、世代・送信ID・本文・画像・Cookie/AP状態をメモリ上で保持して一時停止する。自動APがShortcutsを開く場面は期待されたシーン遷移として一時停止から分離し、AP完了時も非アクティブなら通常の一時停止へ移行する。復帰時は同じ対象とURLを検証し、準備・readiness・送信応答・次スレ遷移を該当ゲートから一度だけ再開する。APコールバック未着時は同一世代で1回だけ再起動し、URL変更や復帰失敗は安全停止する。同UAの次スレはCookie確認を再実行せず「次スレ準備中」を表示し、readiness確認後の送信待機を500msとする。 | Windows static/JavaScript/diff checks pass; Simulator XCTest, IPA delivery, and device confirmation pending |
| Diagnostics / Posting | Shortcutsがデータ通信エラーを表示して`x-error`を返さない場合に備え、自動APのコールバック欠落を20秒で検出して世代付きの通信失敗として解放する。既存のシーン復帰時1回再試行、手動AP、Cookie/IP/投稿状態機械は変更しない。 | Windows static/JavaScript/diff checks pass; Simulator XCTest, IPA delivery, and device confirmation pending |
| Diagnostics | `ログをコピー` now appends a bounded copy-time UA catalog snapshot: catalog/version and counts, selected fixed profile, available/restricted fixed IDs and display names, restriction expiries, and a count of legacy/unknown restriction keys. Raw UA strings remain excluded and the persisted 500-entry log is unchanged. | Windows static/JavaScript/diff checks, XCTest, and IPA delivery pass; device confirmation pending |
| Diagnostics / Posting | 自動クリックと実フォーム送信を分離するため、送信イベントをページトークン・送信ID付きで観測するブリッジを追加。クリック後15秒の応答監視を行い、実送信が観測されない場合だけ同じ試行を送信準備ゲートから1回再試行し、送信観測済みまたは再試行後の無応答は重複送信せず停止する。 | Windows static/JavaScript checks pass; XCTest and device confirmation pending |
| Diagnostics / Posting | 自動投稿の終了所有権、単調増加する送信ID、ページURL付き準備通知、遷移前ブリッジ信号の安全な再利用、複数スレ宛先での本文復元・一致確認、トグルOFF時の未送信作業取消し、独立したカタログ取消しの終端処理を強化。 | Windows static/JavaScript checks and Simulator XCTest pass; IPA delivery pass; device confirmation pending |

Workflow run `#34811055591` completed successfully for commit `f6dc55d`: injected JavaScript validation (10 scripts), XcodeGen generation, iOS Simulator XCTest, unsigned device build, IPA ZIP validation, and Windows self-hosted delivery all passed. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found exactly one `Payload/MiniBrowser.app`, minimum OS `26.0`, bundle identifier `local.sidestore.MiniBrowser`, no `_CodeSignature` or `embedded.mobileprovision`, and SHA-256 `AD02F6649B0E6FADD3AC2B3641A1E348B101C878309CF9BAA09C3403769B5491`. Device confirmation remains required for comment-only, image-only, comment-plus-image repeats, one-second minimum wait plus readiness delay, duplicate completion markers, toggle-off interruption, access/IP/continuous restrictions, and page-transition stopping.

The unit-test bundle and app compilation run on the GitHub Actions macOS runner because UIKit/WebKit iOS targets cannot be compiled on Windows. Workflow run `#6` completed successfully for commit `70ad4b2`: the simulator test bundle compiled, the unsigned device IPA was packaged, and the Windows self-hosted runner delivered it to `%MINIBROWSER_DELIVERY_DIRECTORY%\MiniBrowser.ipa`. Local post-delivery checks confirmed a readable ZIP/IPA structure, one `Payload/MiniBrowser.app/Info.plist`, deployment target 26.0, and no code-signature or embedded provisioning entries.

Workflow run `#7` completed successfully for commit `bcad976` and delivered the focus-mode build to the fixed IPA path. The JavaScript dialog issue was resolved and confirmed on device. TargetPage focus mode and the native toolbar sizing changes still require device confirmation.

Workflow run `#8` completed successfully for commit `a8b4b49` and delivered the input-focus zoom build to the fixed IPA path. Local verification found SHA-256 `1E9F5AE160C17543D17EACBF50837132D04FB27FD2375AA9E17AFA479F3C5CCE`, a valid ZIP structure, deployment target 26.0, and no signature or embedded provisioning entries. Manual pinch zoom remains enabled by design; focus behavior requires device confirmation.

Workflow run `#9` completed successfully for commit `9d89209` and delivered the simplified TargetPage layout build to the fixed IPA path. Local verification found SHA-256 `E12B8CB33DF87E3BA6336882A7A91ECA2941EA492802A4E5FC2475CCE088F207`, a valid ZIP structure, deployment target 26.0, and no signature or embedded provisioning entries. Layout, posting, and own-reply visibility require device confirmation.

Workflow run `#10` completed successfully for commit `8fcefe5` and delivered the compact form and official-list split-view build to the fixed IPA path. Local verification found SHA-256 `E2D64E3830EBB919FE6A29B1B1FB7B500485DC30D15A5030C24CCC4803885C35`, a valid `Payload/MiniBrowser.app` ZIP structure, deployment target 26.0, and no signature or embedded provisioning entries. The split layout, sort switching, draft retention, posting, and own-reply visibility require device confirmation. The reusable workflow is subsequently updated to execute XCTest on an automatically selected available iPhone Simulator instead of only compiling the test bundle.

Workflow run `#11` completed successfully for commit `ddf9838`: all XCTest cases ran on an automatically selected iPhone Simulator, the unsigned device IPA was rebuilt, and the Windows self-hosted runner replaced the fixed delivery file. Local verification found SHA-256 `626320098A0DCB5A4DAA2A59D5E26B1A31417E46640B6AFD085F75524FFFDDAA`, deployment target 26.0, a valid `Payload/MiniBrowser.app` structure, and zero signature or embedded-provisioning entries.

An iPhone 14-class device on iOS 26.5.2 then reported an immediate `SIGABRT` in `swift::AsyncTask::completeFuture` / `__cxa_pure_virtual` while opener texts were loading. The four-child throwing task group was removed in favor of one cancellable sequential loader, eliminating the implicated concurrent future-completion path while retaining bounded Range requests and progressive list updates.

Workflow run `#13` completed successfully for crash-fix commit `bd8682c`, including all unit tests and the 20-second list launch UI smoke test. The Windows runner delivered the corrected IPA at 2026-09-05 14:38:23 JST. Local verification found SHA-256 `5660C8E77871CE1AFB6D4189BBFB5B59466C69AD316068A5AB944A6346A48003`, deployment target 26.0, a valid Payload structure, and no signature or embedded-provisioning entries. Final confirmation of the device-specific crash fix requires reinstalling this build on the reporting device.

The crash fix and posting were subsequently confirmed on the reporting device. Issue #1 was closed from that evidence. List thumbnails now use explicit sequential requests with a list Referer, one retry, and an in-memory success cache instead of `AsyncImage`; selected threads receive a persistent open count and highlighted background. The response-mode heading is hidden and the reply/draft buttons sit above the form table so the handwriting field can remain open.

Workflow run `#15` compiled commit `962bac0`, ran 35 unit tests with zero failures, passed the 37-second list launch test, built the unsigned app, and uploaded its IPA. After a 90% GitHub Actions allowance alert, the run was cancelled at the delivery boundary to stop further hosted-runner use; the already-generated artifact was downloaded and delivered locally without another Actions run. Verification found SHA-256 `25AC8ED8EA16795B755C22523F9AA55828847872AD70D1769CA0E7E5E44E8A58`, deployment target 26.0, one expected app payload, and zero signature/provisioning entries. The IPA workflow is now manual-dispatch-only to avoid spending macOS minutes for intermediate pushes. Issue #2 remains open for device confirmation.

The private-repository workflow now cancels duplicate runs for the same ref and limits the hosted macOS build job to 12 minutes. Codex defaults to GPT-5.6 Luna/MAX for routine work, with documented early escalation to Terra/High or Sol/High for progressively higher-risk implementation and diagnosis.
The 19:01 TargetPage form and list change set was reverted after device testing found a regression: the focused thread view showed the full reply list and list cells could not open their threads. Workflow run `#17` and its IPA remain historical delivery evidence only; a new IPA must be built and delivered from the restored baseline before further device testing. Future TargetPage improvements will be isolated into small, independently verifiable changes.

The first isolated follow-up applies two independent behaviors: the TargetPage deletion-key field remains editable and preserves the value supplied by the page or entered by the user; and the app auto-dismisses only the exact `configured target host` Cookie retry alert. Form-position switching and list loading are intentionally unchanged.

The follow-up workflow dispatch for commit `4bdc9ad` failed before any macOS runner step began, so no IPA was generated or delivered. The latest device IPA remains the restored-baseline build. The immediate runner-level failure follows the earlier GitHub Actions allowance warning; its billing detail is not available through the repository credential used for diagnostics.

The repository was made public by explicit user decision after the private-account Actions quota reached 2,000 of 2,000 minutes. Workflow run `#20` then completed successfully for commit `2dc68e8`: XCTest passed on the iPhone Simulator, the unsigned IPA was built and validated, and the Windows runner replaced `%MINIBROWSER_DELIVERY_DIRECTORY%\MiniBrowser.ipa`. SHA-256 is `12B78B12655D9EAAACD8832B2C0AC361F8B32312B7DAEF6DA9F46284B6630A0A`; the archive contains `Payload/MiniBrowser.app`, declares minimum OS 26.0, and has no signature or embedded provisioning entries. Device confirmation of the deletion-key and exact Cookie-alert behavior remains pending.

The reporting iPhone subsequently confirmed the deletion-key and exact Cookie-alert behavior. The next isolated list change raises the active-thread limit from 30 to 60, excludes threads with 1,000 or more replies, preserves matching thumbnail data across refreshes, and rotates failed thumbnail retries so later cells are not indefinitely starved.

Workflow run `#21` completed successfully for commit `fbf43cc`: iOS Simulator XCTest passed, XcodeGen generated the project, and the unsigned IPA was built, package-validated, and delivered by the Windows runner. Direct verification of `%MINIBROWSER_DELIVERY_DIRECTORY%\MiniBrowser.ipa` found SHA-256 `62652C78DE95A272466F28246C29C9B94EBEAD1EDDDA5BAF8F0417B39F13787D`, one `Payload/MiniBrowser.app`, minimum OS `26.0`, and no `_CodeSignature` or `embedded.mobileprovision` entry. The active-thread filter, 60-item list, and automatic-refresh thumbnail retry remain for device confirmation.

The next isolated change keeps the handwriting bookmarklet's own source and interaction intact. A page bridge copies only a selected supported image into process memory, requests restoration only when the bookmarklet-created `#oejs` canvas exists on another TargetPage thread page, and redraws from the unmodified in-memory source before adding exactly one random pixel. It does not use UserDefaults, LocalStorage, SessionStorage, logs, or network transfer. This change also introduces the black-and-blue bookmark AppIcon asset list.

Workflow run `#22` exposed an unterminated Swift raw-string terminator during iOS compilation and made no IPA or delivery change. The one-character terminator correction in commit `d175683` was verified by workflow run `#23`: simulator tests passed, the unsigned IPA was built and package-validated, and the Windows runner replaced `%MINIBROWSER_DELIVERY_DIRECTORY%\MiniBrowser.ipa`. Direct post-delivery verification found SHA-256 `54F940718F68C9CA54CA7A033412E01A416EA3E7968BD4DCFADEC9BFEE7FADA9`, exactly one `Payload/MiniBrowser.app`, minimum OS `26.0`, `CFBundleIconName` `AppIcon`, and no signature or embedded provisioning entry. Device confirmation is still required for handwriting-image capture, restoration after a thread change/reload, and the new icon appearance.

The next isolated batch keeps the repository private and adds a user-initiated identity-refresh sequence: selected-UA propagation to every native list/IP request, current-host Cookie deletion, the existing AP shortcut callback, then a single reload and Cookie verification. The list pauses during that sequence. It also adds a fixed submit-status slot, immediate ON-draft restoration after submission, an app-owned up/down form placement control, automatic opening of the existing handwriting control only while an in-memory image exists, and high-contrast visited list cells with reply counts. Windows static and JavaScript syntax verification are required before the next single manual IPA workflow dispatch; device confirmation remains pending.

The follow-up form-stability batch removes the app-owned form-position control and its saved state so the reply form always stays beside the opener at the top of a thread. It waits up to two seconds for a page form that arrives late, hides the target page's transient reply-status node without changing the reply action, and captures draft text from the reply-button click because the target page performs an asynchronous post rather than a native form submission. On a successful post, an in-memory bridge reopens a previously open handwriting canvas; selected images retain the existing in-memory-only restore and one-random-pixel behavior. Windows static checks and injected-JavaScript syntax checks passed; device confirmation remains pending.

The static UA catalog expansion is implemented in commit `c63ea00`: IDs 1–100 remain byte-for-byte unchanged, the catalog contains 300 unique fixed profiles, launch-time UA generation was removed, and automatic sessions use a constrained shuffled order while preserving seven-day restrictions and manual canonical order. Windows static checks, injected-JavaScript checks, and `git diff --check` pass. Initial delivery attempts `#35012253156`, `#35013134427`, and `#35013837374` were cancelled after the macOS simulator-test step stopped reporting progress during a GitHub service incident; the incident later resolved.

The first isolated recovery step after that rollback watches the target page's asynchronous `#retmestip` completion marker and reuses the existing in-memory bridge to reopen a canvas after a successful post in the same thread. It does not add a status overlay or change thread navigation, draft retention, or posting mechanics. Windows static checks, JavaScript syntax checks, and `git diff --check` passed; device confirmation is required before an IPA rebuild.

Workflow run `#30` built commit `d3e4a7d`: XcodeGen, iOS Simulator XCTest, unsigned build, package validation, and artifact upload all succeeded. The first delivery attempt found the Windows runner offline; after the runner was restarted with the configured process environment, the delivery-only rerun succeeded. Local verification of `%MINIBROWSER_DELIVERY_DIRECTORY%\MiniBrowser.ipa` found SHA-256 `CD4285090645942AD4CFB5ED9D1C7065158740E34A4CB4ADD1EB5C4D68E74DD9`, one `Payload/MiniBrowser.app/Info.plist`, minimum OS 26.0, and no signature or provisioning entries. Device confirmation of same-thread canvas reopening remains pending.

After that delivery, the Windows runner startup was hardened locally: its logon task now starts a machine-local wrapper that injects the configured delivery directory into the runner process, starts when available, restarts after failure, and does not stop on idle. The wrapper and task are intentionally not committed because they depend on the runner machine's signed-in account and filesystem. The runner was verified to reconnect and reach `Listening for Jobs` without another macOS build.

The next same-thread posting fix checks the current `#retmestip` value immediately after its observer is attached. The target page can create that node and set `完了` within one asynchronous task, so observing only a later mutation missed the completion bridge and left the handwriting field in text mode. Posting mechanics remain unchanged; device confirmation is required for the reopened canvas.

Workflow run `#32` verified commit `5c83b76`: XcodeGen generation, 43 iOS Simulator XCTest cases, unsigned device build, IPA ZIP validation, and Windows self-hosted delivery all succeeded. Local verification of the fixed delivery file found SHA-256 `8E40BBAFD8159D9D2858922ADA18858BEE70B97A0A7438D24E76459F35769BBB`, exactly one `Payload/MiniBrowser.app`, minimum OS `26.0`, and no `_CodeSignature` or `embedded.mobileprovision` entry. Device confirmation of same-thread handwriting-canvas reopening remains pending.

The follow-up canvas recovery fix in commit `f61ab5c` aligns the opener with the target page's actual `<div id="oebtnj">` control and treats a DOM-resident but hidden canvas as closed. It retries the existing control for a bounded interval without changing the bookmarklet source, image storage, or posting flow. Workflow run `#33` attempt 2 passed the Simulator tests, unsigned build, ZIP validation, and Windows delivery. The fixed file was verified at `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` with SHA-256 `5C468DABCA0EF12ECD975B8843BAD9DAA7AD533CBDCD1D0941010F4BDB8DBBF0`, minimum OS `26.0`, bundle `local.sidestore.MiniBrowser`, and no signature or embedded provisioning entries. Device confirmation of same-thread and thread-switch canvas reopening remains required.

The current error/status restoration batch removes the target-page Cookie-alert auto-dismiss rule and forwards only the exact `…` and `完了` markers from the hidden `#retmestip` node to a fixed native overlay. The overlay is outside the WKWebView layout and has a constant frame, so reply, draft, and handwriting controls do not move. Windows static checks, injected-JavaScript syntax checks, and `git diff --check` pass; iOS Simulator compilation and device confirmation remain pending.

Workflow run `#36` completed successfully for commit `ec90916`: XcodeGen generation, iOS Simulator XCTest, unsigned device build, IPA ZIP validation, and Windows delivery all passed. The delivered file is `MiniBrowser.ipa` at the configured directory, with SHA-256 `9FBCB9BE18A3D7FB5BA335E297654BE72E2CE6E722950F97FEEA82D9A7AA2E1E`, one `Payload/MiniBrowser.app`, minimum OS `26.0`, bundle identifier `local.sidestore.MiniBrowser`, and no code-signature or embedded-provisioning entries. The device must confirm the revised Cookie-reload wording and category-only alert observation.

Workflow run `#38` completed successfully for commit `5546af4`: XcodeGen generation, iOS Simulator XCTest (including the automatic-post state-machine tests), unsigned device build, IPA ZIP validation, and Windows delivery all passed. Direct verification of `%MINIBROWSER_DELIVERY_DIRECTORY%\MiniBrowser.ipa` found SHA-256 `FD0DF5CB486926A178171AFB740493183D932AB3A7A0510008B831830F6CCC1B`, one `Payload/MiniBrowser.app`, minimum OS `26.0`, bundle identifier `local.sidestore.MiniBrowser`, and no `_CodeSignature` or `embedded.mobileprovision` entry. Device confirmation remains required for the UA-triggered automatic-post branches, image payload, alert handling, and fixed-status layout.

The automatic-post diagnostics follow-up adds generation-scoped event sequence numbers and elapsed milliseconds, phase-specific Cookie/AP/JavaScript outcomes, stale page-token and callback rejection events, and a passive distinction between the site's completion marker and a later own-response DOM match. Pending-response counts, response counts, and allowlisted match methods are recorded without comment text, Cookie values, image data, alert text, or raw page tokens. A 12-second verification timeout records an accepted-but-unconfirmed result without triggering another post. Windows static and injected-JavaScript syntax checks pass; XCTest, IPA delivery, and device confirmation remain pending.

Workflow run `#34265058547` completed successfully for commit `ebede7d`: XcodeGen generation, iOS Simulator XCTest, unsigned device build, IPA ZIP validation, and Windows self-hosted delivery all passed. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found SHA-256 `9EDDBC799924A3D9F23214B58F4F80684188C16A3A479B423BADA9D08222DD4F`, exactly one `Payload/MiniBrowser.app/Info.plist`, minimum OS `26.0`, bundle identifier `local.sidestore.MiniBrowser`, and no `_CodeSignature` or `embedded.mobileprovision` entry. Device confirmation of the new automatic-post diagnostics remains pending.

The automatic-post timing recovery batch adds a submit-readiness bridge that requires a stable page, form, button, and idle post marker before every automatic click. A final continuous-post restriction now triggers an AP-only reconnect and one fourth attempt without reload or Cookie deletion. Accepted posts show a separate reflection-verification state and end as `完了（反映未確認）` when the own-response DOM match is not observed; no extra submit is triggered. Windows static and JavaScript checks pass; XCTest and device confirmation are pending.

The timing-recovery implementation was verified by workflow run `#34289263179` for commit `4820a6f`: XcodeGen generation, iOS Simulator XCTest, unsigned device build, IPA ZIP validation, and Windows self-hosted delivery all succeeded. The delivered file at `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` has SHA-256 `A02E4196DFD238D294A458789D484C9F3FA2E1A31059ED9A23FD90253FAA639B`, exactly one `Payload/MiniBrowser.app/Info.plist`, minimum OS `26.0`, bundle identifier `local.sidestore.MiniBrowser`, and no `_CodeSignature` or `embedded.mobileprovision` entry. The preceding run `#34288238074` reached the same build and tests but hit the old 12-minute timeout while uploading the SHA-256 artifact; the reusable workflow now allows 20 minutes. Device confirmation of the delayed submit, continuous-limit AP recovery, reflection-unconfirmed status, and form/handwriting retention remains pending.

The catalog header now includes an `閲覧履歴を消去` control. It clears the published open-count dictionary and removes the persisted `ThreadListOpenCounts` value, so all visited highlights and counts disappear immediately and remain reset after relaunch. Workflow run `#34292966073` completed successfully for commit `4c11379`: iOS Simulator XCTest, unsigned IPA build, ZIP validation, and Windows self-hosted delivery all passed. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found SHA-256 `3BA734C92CFFB5427F5A3B887641FA12519141AEFACE071E901E30C7BCFEA9B1`, exactly one `Payload/MiniBrowser.app/Info.plist`, minimum OS `26.0`, bundle identifier `local.sidestore.MiniBrowser`, and no `_CodeSignature` or `embedded.mobileprovision` entry. Device confirmation of the reset button and catalog layout remains pending.

On 2026-09-10 the explicit UA replacement requirement superseded the baseline 50-profile catalog. The catalog now contains 100 fixed, unique iPhone/iPad profiles with catalog version `2`; the first launch after this replacement resets the saved profile selection and clears catalog-specific seven-day restriction IDs, so no legacy UA value or quarantine state is reused. The baseline specification remains the historical 50-profile requirement; this replacement is recorded here as the later product override. Windows static and JavaScript checks, including the 100-profile count and uniqueness guards, are required before the next delivery workflow.

Workflow run `#34402154485` completed successfully for commit `cb7ecad`: iOS Simulator XCTest, unsigned device build, IPA ZIP validation, and Windows self-hosted delivery all passed. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found SHA-256 `20FA0248EEAF6A62FFCDCED55640E06DF38E46A625A8748EB233DF991B47716D`, exactly one `Payload/MiniBrowser.app/Info.plist`, minimum OS `26.0`, bundle identifier `local.sidestore.MiniBrowser`, and no `_CodeSignature` or `embedded.mobileprovision` entry. Device confirmation of the 100-profile catalog and first-launch catalog migration remains pending.

On 2026-09-10 the GitHub repository was renamed to `quietarc-lab/ios-web-shell`. The application display name, bundle identifier, IPA filename, and runtime behavior remain `MiniBrowser`; only repository metadata and local Git remotes were updated.

The page-marker privacy follow-up keeps the product display name, bundle identifier, user-agent catalog, and posting flow unchanged while rewriting app-branded bridge, page-token, DOM, and input-helper markers to a neutral namespace immediately before helper JavaScript is injected. Legacy `MiniBrowser.*` TargetPage storage keys remain unchanged for rollback and data compatibility, so this is marker obfuscation rather than complete anti-fingerprinting. Workflow run `#34407155043` completed successfully for commit `544a7d4`: Windows static checks and injected-JavaScript syntax checks passed, all iOS Simulator XCTest cases passed, the unsigned IPA was built and ZIP-validated, and the Windows self-hosted runner delivered it. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found SHA-256 `9C67C590F2F2D3EA139381E4906CDEAF2F3AE3E42EBB7F7370325752B8663DED`, exactly one `Payload/MiniBrowser.app`, minimum OS `26.0`, bundle identifier `local.sidestore.MiniBrowser`, and no `_CodeSignature` or `embedded.mobileprovision` entry. Device confirmation of the neutral page markers remains pending.

The continuous-limit timing follow-up keeps the existing Cookie/IP retry delays unchanged. For the final fourth attempt after a continuous-post restriction on attempt three, readiness polling now runs in parallel while a monotonic minimum interval of 3.1 seconds from successful AP completion is enforced before the click. The resulting submit delay is recorded as `SUBMIT_DELAY_SCHEDULED`; device and workflow verification remain pending.

The launch-UA follow-up keeps the fixed 100-profile catalog and manual UA rotation intact while selecting one validated mobile WebKit UA during each process cold start. The generated value is held in memory and propagated to the WKWebView, native thread-list requests, and IP checks without triggering Cookie deletion, AP reconnection, reload, or automatic posting. Generated UA restriction results use a per-install salted digest for the existing seven-day quarantine store; raw generated values and digests are not logged. Windows static checks and XCTest are required before the next delivery workflow, followed by cold-launch, foreground-resume, manual-UA, restriction-fallback, and persistent-Cookie device confirmation.

Workflow run `#34678744763` completed successfully for commit `aa54f9e`: XcodeGen generation, iOS Simulator XCTest, unsigned build, IPA ZIP validation, and Windows self-hosted delivery all passed. The delivered `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` contains one `Payload/MiniBrowser.app`, declares minimum OS `26.0`, has bundle identifier `local.sidestore.MiniBrowser`, and contains no `_CodeSignature` or embedded provisioning entry. Direct SHA-256 verification found `CCD19BCC1760EC99DBFD8C7A3F76D785B6F8213CE4CB42FA8F992DE50CEFCF72`. Cold-launch UA generation, foreground-resume stability, manual UA transition, generated restriction fallback, and persistent-Cookie behavior still require device confirmation.

Workflow run `#34688227932` completed successfully for commit `d39c7dd`: iOS Simulator XCTest, unsigned device build, IPA ZIP validation, and Windows self-hosted delivery all passed. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found SHA-256 `650A4D387F52F89D6BEE0A6B52630B6C2B6D277193C65CF4D6F9A66098B6CCEE`, exactly one `Payload/MiniBrowser.app`, minimum OS `26.0`, bundle identifier `local.sidestore.MiniBrowser`, and no `_CodeSignature` or `embedded.mobileprovision` entry. Device confirmation is required for the removal of the stray navigation brackets and unchanged form layout.

The catalog no longer offers the unused `多順` mode. The visible sort labels are `勢順` and `カ順`, while their full names remain available to VoiceOver. A previously persisted `多順` selection falls back to `勢順` without changing the stored thread history.

Workflow run `#34792835848` completed successfully for commit `cb38c0b`: iOS Simulator XCTest, unsigned device build, IPA ZIP validation, and Windows self-hosted delivery all passed. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found SHA-256 `6F93AA3B3A3744F6C97B4078A4083C30D0F2CCFD6011983F6468195DFE7255D3`, exactly one `Payload/MiniBrowser.app`, minimum OS `26.0`, bundle identifier `local.sidestore.MiniBrowser`, and no `_CodeSignature` or `embedded.mobileprovision` entry. Device confirmation of the two remaining catalog buttons is pending.

The exact TargetPage alert `このスレッドには書けません` now removes the affected thread from the native catalog and persists only its thread ID with a six-hour expiration. Expired entries are purged during initialization and before refresh, so the exclusion store cannot grow indefinitely. The alert remains a normal OK dialog for manual operation; during an automatic post it stops that generation while preserving the same alert behavior. Windows static checks, JavaScript checks, XCTest, and IPA delivery passed; device confirmation remains pending for this change.

The automatic-post diagnostics follow-up records preparation timeout stage flags, JavaScript false or invalid-result outcomes, invalid and unsupported bridge payloads, repeat-image preparation paths, late completion notifications, and session/cycle context for repeat-mode events. It continues to omit comment text, Cookie values, image data, alert text, and raw page tokens. Windows static checks, injected-JavaScript syntax checks, and `git diff --check` pass; XCTest, IPA delivery, and device confirmation remain pending for this follow-up.

Workflow run `#34794668365` completed successfully for commit `dba77f7`: iOS Simulator XCTest, unsigned device build, IPA ZIP validation, and Windows self-hosted delivery all passed. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found SHA-256 `08B3DE8A1C1C4E77400FAC8477F09EC2FF648F3FD8F2393C3CCCA91B6DB6C4ED`, exactly one `Payload/MiniBrowser.app/Info.plist`, minimum OS `26.0`, and no `_CodeSignature` or `embedded.mobileprovision` entry. Device confirmation of exact alert handling, six-hour expiration, and list refresh behavior remains pending.

The alert-diagnostics follow-up records the TargetPage alert message in the local debug log for both known and unknown `img.2chan.net` alerts. Messages are kept on one line, limited to 512 characters, and pass the existing secret-assignment sanitizer; non-TargetPage unknown alert text remains excluded. Cookie values, form contents, image data, and page tokens are still not logged. Windows static and JavaScript checks pass; XCTest, IPA delivery, and device confirmation remain pending.

The same-thread repeat timing follow-up keeps the readiness gate and its 500 ms stability check, but reduces the repeat-cycle start delay to 250 ms and removes the additional 2-second submit delay for same-page repeats. Cookie deletion, AP reconnect, page reload, and the timing of the regular automatic-post branches remain unchanged. The scheduled delays are recorded for device-log comparison.

The same-thread repeat recovery follow-up classifies variable-count `画像の投稿が多すぎます(N枚).` alerts. During same-thread mode it dismisses the alert, preserves the in-memory draft/image session, and hands off to the next untried UA; repeated alerts continue the handoff until no eligible UA remains. Same-thread continuous-post alerts now use AP-only recovery and require an actual IP change before retrying. The normal automatic-post flow and `このスレッドには書けません` handling remain unchanged. Workflow run `#34839133950` passed all 99 XCTest cases, built and ZIP-validated the unsigned IPA, and delivered it through the Windows runner. The delivered file has SHA-256 `23C9F56457C45E3C90390DDE7FFAF7EE09DFF3885D13F80D8F203A9C378B4DBD`, minimum OS 26.0, one `Payload/MiniBrowser.app`, and no signature or provisioning entries. Device confirmation remains pending.

The submit-response recovery follow-up observes the actual TargetPage form `submit` event with a per-click ID and carries that active ID through completion/status bridge messages. A 15-second generation-scoped response watchdog now retries once only when no form submission was observed; if a form submission or own-response DOM evidence exists, timeout stops without another click to avoid a duplicate post. Completion, known/unknown alerts, navigation, and generation changes cancel the watchdog. The submitted form and existing in-memory draft/image remain available for manual fallback. Windows static checks, injected-JavaScript syntax checks, and `git diff --check` pass. Workflow run `#34843618317` completed successfully for commit `2eff0e8`: Simulator XCTest, unsigned device build, IPA ZIP validation, and Windows self-hosted delivery all passed. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found SHA-256 `8BCB59735E3B36D371DD85C3237460B9654411D985FCA0369EBA81513D5928CF`, exactly one `Payload/MiniBrowser.app`, minimum OS `26.0`, bundle identifier `local.sidestore.MiniBrowser`, and no `_CodeSignature` or `embedded.mobileprovision` entry. Device confirmation of the watchdog, manual fallback, and delayed completion race remains pending.

The multi-thread automatic-post follow-up adds a non-persistent `複数スレ` catalog toggle. A session snapshots up to 60 current targets, preserves the in-memory draft/image, posts each target once in snapshot order, waits at least three seconds before navigation to the next target, skips `このスレッドには書けません` targets for six hours, and performs one final catalog refresh for unprocessed IDs. Existing per-generation Cookie/IP/continuous recovery and restriction-driven UA handoff remain in place; active-session controls are locked and stale generation/page callbacks are ignored. Windows static/JavaScript checks and Simulator XCTest pass; device confirmation remains pending.

Workflow run `#34864267053` completed successfully for commit `c7a0066`: XcodeGen generation, Simulator XCTest, unsigned device build, IPA ZIP validation, and Windows self-hosted delivery all passed. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found SHA-256 `6BCE187C813E5DA4FCC7776ECFE82F4091F1432516B633726AA8885FBDC13754`, exactly one `Payload/MiniBrowser.app`, minimum OS `26.0`, and no `_CodeSignature` or `embedded.mobileprovision` entry. Device confirmation of snapshot ordering, three-second transition, restriction handoff, six-hour skip, one-shot refresh, and toggle-off behavior remains pending.
The foreground idle-timer follow-up disables `UIApplication.isIdleTimerDisabled` only while an automatic generation, same-thread repeat session, or accepted-post visibility check still has work pending. Scene deactivation/background transitions restore the default idle timer, and completion or stop clears it as well; this prevents foreground auto-lock from interrupting the flow without claiming background execution. Windows static checks and `git diff --check` are required before the next single macOS workflow dispatch, followed by Simulator XCTest and device confirmation of foreground lock behavior.

Workflow run `#34847219694` completed successfully for commit `14d1b25`: XcodeGen generation, iOS Simulator XCTest including the idle-timer policy tests, unsigned device build, IPA ZIP validation, and Windows self-hosted delivery all passed. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found SHA-256 `D2453ECCF763407AF09AB72BE47B4B3BCCAF0CBBE9D6A767BA10E648F225A2BD`, exactly one `Payload/MiniBrowser.app`, minimum OS `26.0`, bundle identifier `local.sidestore.MiniBrowser`, and no `_CodeSignature` or `embedded.mobileprovision` entry. Device confirmation of foreground idle-timer behavior remains pending.

The observed exact TargetPage alert `画像連続投稿はもうしばらく時間を置いてからお願い致します` (with or without the final Japanese full stop) is classified separately from the ordinary continuous-post alert. During same-thread repeat it follows the existing image-count restriction handoff to the next untried UA; outside same-thread mode the normal alert behavior is preserved. Windows static checks, JavaScript checks, `git diff --check`, and XCTest remain required before delivery.

Workflow run `#34896477765` completed successfully for lifecycle-hardening commit `50c24d3`: XcodeGen generation, iOS Simulator XCTest, unsigned device build, IPA ZIP validation, and Windows self-hosted delivery all passed. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found SHA-256 `696800B1FA7C4584A3BE51FCE96C53E44C48B854EE5D9006B9E8E764830D88B1`, exactly one `Payload/MiniBrowser.app/Info.plist`, minimum OS `26.0`, bundle identifier `local.sidestore.MiniBrowser`, and no `_CodeSignature` or `embedded.mobileprovision` entry. Device confirmation of terminal cancellation, destination draft equality, delayed bridge ordering, and catalog-cancellation behavior remains pending.

The same-thread repeat visibility follow-up keeps own-response DOM confirmation diagnostic after a successful site completion marker. This prevents the visibility callback from finalizing the generation and cancelling the already scheduled repeat session. Single automatic posts, multi-thread posts, and repeat-toggle-off termination retain their existing finalization behavior. Workflow run `#34906089867` (`#70`) completed successfully for commit `9eb1c45`: Windows static checks, injected-JavaScript syntax checks, `git diff --check`, XcodeGen generation, iOS Simulator XCTest, unsigned device build, IPA ZIP validation, and Windows self-hosted delivery all passed. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found SHA-256 `BCF806A3521173539482F30D68603E9962483381FC2EBD63419803CC2EC8A97C`, exactly one `Payload/MiniBrowser.app`, minimum OS `26.0`, bundle identifier `local.sidestore.MiniBrowser`, and no `_CodeSignature` or `embedded.mobileprovision` entry. Device confirmation of the repeat race fix remains pending.

The next-thread image-preparation race fix binds a `canvasReady` restoration started before `didFinish` to the destination URL and page token, then accepts exactly one generationless `handwritingReady` callback for that destination after the new generation starts. Frame request URLs are used for the binding, and pending contexts are cleared on navigation, UA/session termination, or flow completion so stale image restorations cannot leak into later targets. Workflow run `#34919612651` completed successfully for commit `74a6e7d`: Windows static and injected-JavaScript checks, `git diff --check`, XcodeGen generation, iOS Simulator XCTest, unsigned device build, IPA ZIP validation, and Windows self-hosted delivery all passed. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found SHA-256 `4A4C5CF50DEAFA15C5188E0C670C25AF7B2F3F146F009FC89D70119BB9E74326`, exactly one `Payload/MiniBrowser.app`, minimum OS `26.0`, bundle identifier `local.sidestore.MiniBrowser`, and no `_CodeSignature` or `embedded.mobileprovision` entry. Device confirmation of image restoration on a next-thread transition remains pending.

The multi-thread target-selection follow-up filters the start and one-shot refresh snapshots to catalog entries with no recorded viewing history, allowing fewer than 60 targets. It retains reply counts, skips stale 1,000-reply targets, and records a six-hour catalog exclusion for those entries alongside the existing exact `このスレッドには書けません` exclusion. A multi-thread session records accepted target posts and starts a fresh UA generation after every two successful targets; skipped targets do not consume the two-target batch. Windows static checks and `git diff --check` are required before the next delivery workflow, followed by Simulator XCTest and device confirmation of snapshot filtering, two-target UA rotation, and stale reply-limit handling.

Workflow run `#34923697926` completed successfully for commit `8ea63de`: XcodeGen generation, iOS Simulator XCTest, unsigned device build, IPA ZIP validation, and Windows self-hosted delivery all passed. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found SHA-256 `D4B8E0E310010B73FDEE7B9E7D62BC77F949053D675F33DB4456836A0DC50936`, exactly one `Payload/MiniBrowser.app`, minimum OS `26.0`, bundle identifier `local.sidestore.MiniBrowser`, and no `_CodeSignature` or `embedded.mobileprovision` entry. Device confirmation of history-free snapshot filtering, two-target UA rotation, stale reply-limit handling, and next-thread image restoration remains pending.

Successful posts in same-thread and multi-thread continuous modes now mark the target as read in the existing `ThreadListOpenCounts` history. The update is idempotent, preserves any existing view count, immediately drives the catalog highlight, and causes later history-free snapshots to exclude the target. One-off automatic posts remain unchanged. Windows static checks and `git diff --check` pass; XCTest, IPA delivery, and device confirmation of the persisted highlight remain pending.

Workflow run `#34929693769` completed successfully for commit `417bbb1`: XcodeGen generation, iOS Simulator XCTest, unsigned device build, IPA ZIP validation, and Windows self-hosted delivery all passed. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found SHA-256 `A44EC07CB57801EA543A21042FFFF710404864999F784C4A67C8B47CC18E5116`, exactly one `Payload/MiniBrowser.app`, minimum OS `26.0`, bundle identifier `local.sidestore.MiniBrowser`, and no `_CodeSignature` or `embedded.mobileprovision` entry. Device confirmation of automatic-success highlighting and history-free snapshot exclusion remains pending.

The dropped-thread follow-up treats a multi-thread destination without a usable TargetPage thread/form, a navigation mismatch, navigation failure, or navigation timeout as a per-target skip. The target is excluded for six hours, any in-flight generation cleanup is invalidated, and the session advances to the next snapshot entry without changing manual or single-thread behavior. Workflow run `#34964603815` completed successfully for commit `21110f4`: Windows static checks and injected-JavaScript syntax checks passed, all iOS Simulator XCTest cases passed, the unsigned IPA was built and ZIP-validated, and the Windows self-hosted runner delivered it. Direct verification found SHA-256 `633D64E713B3CB8A1F4419F01E928C76293346A05359CEA40627A093D204BBC6`, exactly one `Payload/MiniBrowser.app`, no `_CodeSignature` or `embedded.mobileprovision` entry, and the workflow's package validation confirmed minimum OS `26.0`. Device confirmation of a dropped target, six-hour exclusion, and continuation to the next target remains pending.

Workflow run `#35027449947` completed successfully for commit `8a37ae2`: all iOS Simulator XCTest cases, unsigned device build, IPA ZIP validation, artifact SHA verification, and Windows self-hosted delivery passed. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found SHA-256 `817B655C8624295592FDFECF4891FA05715EE51896034C76AAD0BF4B94AC6F65`, exactly one `Payload/MiniBrowser.app`, minimum OS `26.0`, bundle identifier `local.sidestore.MiniBrowser`, and no `_CodeSignature` or `embedded.mobileprovision` entry. Device confirmation of the expanded catalog, restricted-UA display/routing, and automatic family-aware rotation remains pending.

The short continuous-post alert `連続投稿はしばらく時間を置いてからお願い致します` (with or without the final Japanese full stop) is now classified as the existing continuous-post category. In the multi-thread final-attempt path this reaches the existing AP-only recovery, requires a changed IP, waits the configured post-AP interval, and retries once; unrelated or embellished messages remain unknown alerts. Windows static/JavaScript checks, `git diff --check`, and XCTest are required before delivery.

Workflow run `#35043885720` completed successfully for commit `ca5206f`: Windows static checks, injected-JavaScript syntax checks, `git diff --check`, iOS Simulator XCTest, unsigned device build, IPA ZIP validation, and Windows self-hosted delivery all passed. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found SHA-256 `4F55EC417DC6DC86C67C20F5D7572FAC5D98E22FC77C6C6A04B26D4A13302330`, exactly one `Payload/MiniBrowser.app/Info.plist`, minimum OS `26.0`, bundle identifier `local.sidestore.MiniBrowser`, and no `_CodeSignature` or `embedded.mobileprovision` entry. Device confirmation of the short continuous-post alert and AP-only recovery remains pending.

The same-thread repeat UA-handoff fix preserves the repeat-mode flag when a restriction starts a new automatic generation, including the initial same-thread session. This keeps image-count and image-continuous alerts on the existing next-UA path after handoff instead of treating the second alert as an unknown error. Workflow run `#35045492211` completed successfully for commit `1b027fe`: iOS Simulator XCTest, unsigned device build, IPA ZIP validation, and Windows self-hosted delivery all passed. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found SHA-256 `DEC7F734CD1B61272F40A0F4CAE9FF4872EED351CCEE40FA56946AD3901480DC`, exactly one `Payload/MiniBrowser.app/Info.plist`, minimum OS `26.0`, bundle identifier `local.sidestore.MiniBrowser`, and no `_CodeSignature` or `embedded.mobileprovision` entry. Device confirmation of repeated image restrictions across UA generations remains pending.

The exact TargetPage continuous-post alert without the final Japanese full stop, `連続投稿はもうしばらく時間を置いてからお願い致します`, is now classified alongside its punctuated form. This prevents the observed same-thread repeat alert from falling through to `UNKNOWN_ALERT`; existing retry and AP/UA recovery behavior is unchanged. Workflow run `#35124916248` completed successfully for commit `3a8b3cf`: Simulator XCTest, unsigned device build, IPA ZIP validation, and Windows self-hosted delivery all passed. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found SHA-256 `B690354DF4322E6AD7C9B32861950D0B774C13EA7339BA728A7195637D797192`, exactly one `Payload/MiniBrowser.app`, minimum OS `26.0`, bundle identifier `local.sidestore.MiniBrowser`, and no `_CodeSignature` or `embedded.mobileprovision` entry. Device confirmation of the unpunctuated alert path remains pending.
The dropped-thread navigation follow-up adds a generationless, read-only availability probe at the end of each expected multi-thread transition. A target URL that lacks either the TargetPage thread container or reply form is skipped as one unavailable catalog entry, with session, URL, navigation-sequence, and pending-bridge checks preventing stale callbacks from advancing a later target. Navigation mismatches continue to skip only the current target; ordinary, manual, and same-thread flows are unchanged. Workflow run `#35219704788` completed successfully for commit `23f6ebe`: Windows static checks, injected-JavaScript checks, `git diff --check`, XcodeGen generation, iOS Simulator XCTest, unsigned IPA build, IPA ZIP validation, and Windows self-hosted delivery all passed. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found SHA-256 `B0DCDC89CCEDD7DE5DB91F8FA31EBEEF3EDD42B03BA8BFF57EBCE428482B3A1A`, exactly one `Payload/MiniBrowser.app`, minimum OS `26.0`, bundle identifier `local.sidestore.MiniBrowser`, and no `_CodeSignature` or `embedded.mobileprovision` entry. Device confirmation of dropped-thread continuation remains pending.

The continuous-AP retry and multi-thread pacing follow-up allows up to three same-generation AP reconnect attempts when the post-reconnect IPv4 is unchanged, with one second between unchanged-result retries; Shortcut or IPv4-check failures still stop immediately. Successful multi-thread targets now wait one second before navigating to the next target, while skipped targets remain immediate. Workflow run `#35232652346` completed successfully for commit `d5af69b`: Windows static and injected-JavaScript checks, `git diff --check`, XcodeGen generation, iOS Simulator XCTest, unsigned device build, IPA ZIP validation, and Windows self-hosted delivery all passed. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found SHA-256 `E506AAE332F8672BBBBF25B62643B34DC2373AF3FFF2823DE9E468213DF28C03`, exactly one `Payload/MiniBrowser.app`, minimum OS `26.0`, and no `_CodeSignature` or `embedded.mobileprovision` entry. Device confirmation of unchanged-IP retry behavior and one-second multi-thread pacing remains pending.

The submit-response timeout follow-up adds one bounded Cookie/page rebuild only when the single same-page response retry also times out after a Cookie-retry alert. The in-memory draft and image session are retained, late callbacks are invalidated by a new generation and the previous page token, and no UA or AP change is performed. A second Cookie rebuild is not allowed in the same recovery chain; missing Cookies or failed reload/preparation stop the operation. Windows static and injected-JavaScript checks, `git diff --check`, and Simulator XCTest remain required before delivery; device confirmation remains pending.

Workflow run `#35260249725` completed successfully for commit `6b4d991`: XcodeGen generation, iOS Simulator XCTest, unsigned device build, IPA ZIP validation, and Windows self-hosted delivery all passed. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found SHA-256 `C68BB6156FCFB323A59956227B5361C6E2F5359B48F0068222CB7D6C2066263D`, exactly one `Payload/MiniBrowser.app`, minimum OS `26.0`, and no `_CodeSignature` or `embedded.mobileprovision` entry. Device confirmation of the bounded Cookie/page rebuild remains pending.

The catalog header now has a small non-persistent isolation-stop checkbox immediately to the left of `同スレ連続`; it starts enabled on each process launch. While either automatic continuous mode is active and the app is foregrounded, a native conditional-GET monitor reads Futapo's `img_b_isolation.txt` feed every ten seconds. Same-thread mode monitors the displayed thread URL, while multi-thread mode monitors every valid `img.2chan.net/b/res/<id>.htm` URL captured in the in-memory post body. An exact feed match stops the automatic session with `STOPPED_ISOLATED_THREAD`; feed failures retain the last valid set and retry, and disabling the checkbox cancels monitoring without changing posting or manual-alert behavior. Only normalized thread IDs and error domain/code diagnostics are retained. Simulator and device confirmation of the monitor, feed availability, and checkbox interaction remain pending.

Workflow run `#35266035984` completed successfully for commit `c4ca4d1`: Windows static checks, injected-JavaScript checks, `git diff --check`, iOS Simulator XCTest, unsigned device build, IPA ZIP validation, and Windows self-hosted delivery all passed. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found SHA-256 `77A89F6A54F3678DD9E5D9691E0002D7E2630BA3F8EDD8907E72490F346F1FBA`, exactly one `Payload/MiniBrowser.app/Info.plist`, `MinimumOSVersion` `26.0`, bundle identifier `local.sidestore.MiniBrowser`, and no `_CodeSignature` or embedded provisioning entry. Device confirmation of the Futapo feed, foreground polling, exact-match stop, and checkbox interaction remains pending.

The multi-thread continuous-limit recovery follow-up no longer ends the entire session when the short `連続投稿` restriction reaches the current generation's retry limit. For each target, the first terminal occurrence preserves the in-memory draft/image and hands off to the next unused UA generation; if the same target reports the restriction again after that handoff, only that target is marked processed and the session advances without removing it from the six-hour catalog exclusion store. Ordinary automatic and same-thread repeat flows retain their existing terminal stop behavior. Workflow run `#35291421036` completed successfully for commit `5b03740`: Windows static checks, injected-JavaScript checks, `git diff --check`, 141 iOS Simulator XCTest cases, unsigned device build, IPA ZIP validation, and Windows self-hosted delivery all passed. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found SHA-256 `7DADA5C39A7BAA02FFCCD64F261ADD8472A8F178E2FF3B3152E3E47F845316FA`, exactly one `Payload/MiniBrowser.app`, minimum OS `26.0`, and no `_CodeSignature` or `embedded.mobileprovision` entry. Device confirmation of repeated short continuous-limit alerts and per-target continuation remains pending.

The multi-thread snapshot follow-up now reuses the isolation monitor's exact post-body URL parser to exclude every catalog target referenced by a retained post-body thread URL. The exclusion is held in memory for the session, applies to the initial history-free snapshot and its one-shot refresh, and does not change same-thread repeat or ordinary automatic target selection. Unit coverage verifies both initial filtering and protection against reintroducing an excluded ID during refresh; device confirmation remains pending.

The multi-thread traversal follow-up replaces the one-shot 60-entry session with alternating non-persistent batches: momentum starts at 20 targets per batch with a 60-target phase limit, catalog follows at 10 per batch with a 30-target phase limit, and the phases continue alternating until both sorts produce no new eligible target. Selection, successful posts, 1,000-reply skips, unavailable-thread skips, read highlights, retained post-body URL exclusions, six-hour exclusions, and two-post UA rotation remain session-scoped. The isolation monitor now raises a native reason-specific `隔離検知のため自動投稿を停止しました` or `削除検知のため自動投稿を停止しました` confirmation alert that remains until OK and never resumes the stopped session; an overlapping feed match preserves the isolation classification. Windows static/JavaScript checks and Simulator XCTest are required before the next delivery workflow; device confirmation remains pending.

The moderation-stop follow-up classifies Futapo's field-16 feed state (`2` isolation, `1` deletion) into an in-memory `ModerationThreadSnapshot`. The existing session-only checkbox now covers both conditions; exact matches in the displayed same-thread URL or retained multi-thread post-body URLs stop posting, navigation, catalog fetches, and monitoring. A reason-specific native alert remains until OK and does not resume the session; while it is pending, a short generated in-app tone and error haptic repeat every two seconds and stop on confirmation. Conditional-GET failures retain the last valid snapshot and retry. Critical Alerts, local notifications, lock-screen delivery, and background monitoring remain out of scope; device confirmation of feed classification and audio/haptic behavior remains pending.

Workflow run `#35296944910` completed successfully for commit `bcab3cf`: Windows static checks, injected-JavaScript checks, `git diff --check`, iOS Simulator XCTest, unsigned device build, IPA ZIP validation, and Windows self-hosted delivery all passed. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found SHA-256 `1B06B598FDEB18BE3F4F23FE8EB8870F0FF558AD56048F984D54851EB8D483C1`, exactly one `Payload/MiniBrowser.app`, `MinimumOSVersion` `26.0`, bundle identifier `local.sidestore.MiniBrowser`, and no `_CodeSignature` or `embedded.mobileprovision` entry. Device confirmation of retained post-body URL exclusion remains pending.

The scene-lifecycle follow-up pauses automatic posting when the app leaves the active scene and resumes the same generation, target, draft, image, and state gate when the app returns. Preparation, readiness, response, transition, and continuous-AP timers are suspended while inactive; an AP callback that is still missing after resume is retried once for the same generation and purpose, then stops as a communication failure. A manual URL or target change while away causes a safe stop, and the isolation/deletion monitor is resumed on return. Workflow run `#35429841251` completed successfully for commit `4becd09`: Windows static checks, injected-JavaScript checks, `git diff --check`, iOS Simulator XCTest, unsigned device build, IPA ZIP validation, and Windows self-hosted delivery all passed. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found SHA-256 `E506A7A0C7C5D8337DC5F5C6411A384E62F2712AE26FC7DBA0AB8C9DE40E2E7B`, exactly one `Payload/MiniBrowser.app`, `MinimumOSVersion` `26.0`, bundle identifier `local.sidestore.MiniBrowser`, and no `_CodeSignature` or embedded provisioning entry. Device confirmation of scene suspension, foreground resume, and AP callback timing remains pending.

Unexpected terminal automatic-post failures now use the same confirmation-required native alert and repeating in-app tone/error-haptic path as moderation stops. Communication failures, preparation/readiness failures, unknown or exhausted alerts, retry exhaustion, unavailable UA, catalog refresh failures, and failed scene restoration are alertable; manual stops, normal completion, ordinary multi-thread skips, and normal scene pause/resume remain silent. Confirming the alert stops the feedback and does not restart the session. Device confirmation of the failure-alert wording and repeated audio/haptic behavior remains pending.

Workflow run `#35541280336` completed successfully for commit `65a7e16`: Windows static checks, injected-JavaScript checks, 157 iOS Simulator XCTest cases, one UI launch test, unsigned device build, IPA ZIP validation, and Windows self-hosted delivery all passed. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found exactly one `Payload/MiniBrowser.app`, no `_CodeSignature` or `embedded.mobileprovision` entry, and SHA-256 `784AD49339F22D11EBF044DF6540A9F971D99544E8AFBE0DFEA71F49722BF405`; the project deployment target is iOS 26.0. Device confirmation of unexpected-stop alert wording and repeated audio/haptic behavior remains pending.

The live `上限1000レスに達しました` TargetPage alert is now classified separately. In multi-thread mode it is auto-dismissed, the current target is marked processed and excluded from the catalog exclusion store, and the session advances to the next target without invoking the terminal red failure alarm. Single-thread and same-thread repeat flows retain their existing terminal stop behavior; unrelated or embellished messages remain unknown alerts.

The transient TargetPage proxy-error follow-up classifies HTTP 502/503/504 responses and the observed `Proxy Error` page text (`Error reading from remote server`) without recording page text. Multi-thread mode marks the affected target processed, applies the existing catalog exclusion, and continues to the next target; same-thread mode waits one second and reloads the same URL once, then stops through the existing communication-failure path if the error recurs. The retry is scoped to one generation and URL and does not rotate UA, delete Cookies, or reconnect AP. Workflow run `#35770861428` completed successfully for commit `bfa1736`: Windows static/JavaScript checks, `git diff --check`, iOS Simulator XCTest, unsigned device build, IPA ZIP validation, and Windows self-hosted delivery all passed. Direct verification of `C:\Users\staka\iCloudDrive\Downloads\MiniBrowser.ipa` found SHA-256 `12DACF74B475B1E65B46A8BB3C2E0CAED891F45E5560F92D08859F93C6D0F67C`, exactly one `Payload/MiniBrowser.app/Info.plist`, minimum OS `26.0`, and no `_CodeSignature`, `embedded.mobileprovision`, or entitlements entry. Device confirmation of proxy-page classification and continuation remains pending.

The isolated-link recovery flow now follows only an exact rendered two-line `次` then thread-URL pattern, watches the source page for at most two minutes, and reports a bounded no-link failure instead of remaining active indefinitely. It canonicalizes the replacement URL, changes only matching source-thread URL tokens in the in-memory post body, captures the new thread's starter image into the existing handwriting image store, clears open/read history, and bootstraps a fresh multi-thread session. Manual navigation away cancels recovery; the isolated-thread stop alert/alarm stays active until the user acknowledges it. Windows static and executable JavaScript fixture checks pass; Simulator XCTest, unsigned IPA delivery, and device confirmation remain pending.
